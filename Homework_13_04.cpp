// 4. Обидва цикли виходять за межі масиву, оскільки відлік елементів
//    починається з 0, а не 1. Отже, необхідно замінити знаки <= на < .

	unsigned short SomeArray[5][4];
	for (int i = 0; i <= 5; i++)
    		for (int j = 0; j <= 4; j++)
	        	SomeArray[i][j] = 0;
